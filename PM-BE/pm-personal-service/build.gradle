dependencies {
    api project(':pm-upload-service')
    api project(':pm-domain')
    api project(':pm-user-service')

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

test {
    useJUnitPlatform()
}
