dependencies {
    api project(':pm-domain')
    api project(':pm-user-service')
    api project(':pm-search-service')

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

test {
    useJUnitPlatform()
}
